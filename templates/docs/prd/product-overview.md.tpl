# 产品概览

## 产品定位

`{{PROJECT_NAME}}` 的产品定位待确认。不得根据项目名、默认 README 模板或目录名推断业务事实。

如果本项目是既有项目接入治理，产品定位应在后续任务中基于代码证据、用户确认和可验证行为逐步回填；无法确认的内容继续保留为 `待确认`。

## 核心场景

- 待确认。

## 关键业务规则

- PRD 只描述当前真实行为，不记录愿望、计划或未经确认的猜测。
- 历史决策和变更过程写入 `docs/prd/prd_log/YYYYMMDD-{git-user}.md`，不写入模块当前行为文档。
- 业务规则待确认。

## 非目标/边界

- 待确认。

## 当前已知风险

- 初始化模板中的业务内容均为待确认，需要结合真实代码、配置和用户说明填充。
