# 20260704-dumpling PRD / 开发变更日志

## 2026-07-04 - 初始化 PRD-first 可审计 AI 研发闭环

### 编写人
- dumpling

### 请求
- 为 `prd-keeper-bootstrap` 初始化通用 PRD-first 可审计 AI 研发闭环。

### 影响模块
- AI 入口文件。
- 项目内 PRD Keeper Skill。
- 分层 PRD 文档。
- PRD 日志与校验脚本。

### 代码变更
- 新增或更新 `AGENTS.md`、`CODEX.md`、`CLAUDE.md` 薄入口。
- 新增或更新 `.agent/skills/prd-keeper/` 项目内治理 Skill。
- 新增或更新 `docs/prd/` 分层 PRD 模板。

### PRD 更新
- 创建初始 PRD 总入口、产品概览、架构说明、模块索引和 PRD Keeper 模块文档。
- 回填本项目自身的 Baseline PRD：产品定位、核心场景、运行入口、系统结构、关键数据流和 Bootstrap 安装器模块。
- 新增 `docs/prd/modules/bootstrap-installer.md`，记录 `scripts/init_prd_keeper.py` 与 `templates/` 的当前行为和边界。
- 更新 `.agent/skills/prd-keeper/references/project-map.md` 和 `references/gotchas.md`，记录已确认项目结构和 Windows Python 入口风险。

### 验证
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py`，结果为 `prd-keeper validation passed`。
- 待运行：PRD 回填完成后，使用 `--log-file docs\prd\prd_log\20260704-dumpling.md` 再次执行日志级校验。

### 风险/后续事项
- 需要确认 README 末尾 MIT License 建议与根目录 GPL-3.0 `LICENSE` 是否统一。
- Windows 环境中 `python` 可能先命中 WindowsApps 占位入口；本次通过临时把 `C:\Users\44103\AppData\Local\Python\bin` 放到 PATH 前面执行。
- Roadmap 中 `scan`、`upgrade`、`--dry-run`、配置化初始化和 CI 校验的优先级尚未确认。

## 2026-07-04 - 新增 npm 安装与命令包装入口

### 编写人
- dumpling

### 请求
- 将 `prd-keeper-bootstrap` 扩展为可通过 npm 安装和运行的工具，并说明需要用户手动处理的发布事项。

### 影响模块
- Bootstrap 安装器。
- README 快速开始与发布说明。
- PRD 总入口、产品概览、架构说明、模块索引、Bootstrap 安装器模块文档。
- 项目地图与可复用坑点。

### 代码变更
- 新增 `package.json`，声明 npm 包名 `prd-keeper-bootstrap`、版本 `0.1.0`、Node.js >= 18、`prd-keeper` / `prd-keeper-bootstrap` 两个 bin 命令、发布文件集合和 GPL-3.0-only 许可证。
- 新增 `bin/prd-keeper.js`，提供 `--help`、`--version`、`init` 命令；支持直接以 `--target` 等参数调用时默认进入 `init`；探测 `PRD_KEEPER_PYTHON`、Windows `py -3` / `python` / `python3` 或非 Windows `python3` / `python`，确认 Python >= 3.9 后转发到 `scripts/init_prd_keeper.py`。
- 更新 README，将 npm 全局安装、npx 使用、源码脚本三种入口写入快速开始，并将许可证说明改为与根 `LICENSE` 一致的 GPL-3.0。

### PRD 更新
- 更新 `docs/prd/README.md`，记录 npm、npx、Python 源码三种入口和 npm 包装器职责。
- 更新 `docs/prd/product-overview.md`，记录 npm 使用场景、Node/Python 前置要求、`PRD_KEEPER_PYTHON` 和 npm 包名发布风险。
- 更新 `docs/prd/architecture.md`，记录 Node.js 包装器、`package.json`、npm 数据流、环境变量和发布边界。
- 更新 `docs/prd/modules/README.md` 与 `docs/prd/modules/bootstrap-installer.md`，将 npm 包装入口纳入 Bootstrap 安装器模块当前行为。
- 更新 `.agent/skills/prd-keeper/references/project-map.md` 和 `references/gotchas.md`，记录新增结构、命令和 npm 包名/许可证注意事项。

### 验证
- 已运行：`npm.cmd run cli:help`，结果输出 `PRD Keeper Bootstrap` 帮助信息。
- 已运行：`node .\bin\prd-keeper.js --version`，结果输出 `0.1.0`。
- 已运行：设置 `PRD_KEEPER_PYTHON=C:\Users\44103\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` 后执行 `node .\bin\prd-keeper.js init --help`，结果成功转发到 `init_prd_keeper.py` 并输出 Python 脚本帮助。
- 已运行：`node --check .\bin\prd-keeper.js`，结果通过。
- 已运行：`npm.cmd pack --dry-run` 与 `npm.cmd run pack:check`，结果显示 npm 包会包含 `bin/`、`scripts/`、`templates/`、README、LICENSE、根 `SKILL.md` 和根 `routing.yaml`，共 30 个文件。
- 已运行：`npm.cmd view prd-keeper-bootstrap version`，npm registry 返回 E404，当前未找到公开的 `prd-keeper-bootstrap` 包或当前账号无权限访问它。
- 已运行：`npm.cmd pack --pack-destination .`，生成 `prd-keeper-bootstrap-0.1.0.tgz`，真实 tarball 包含 30 个文件；同时将 `*.tgz` 加入 `.gitignore`，避免发布产物进入版本控制。
- 已运行：`npm.cmd publish --dry-run`，首次提示 `bin` 路径被 npm 自动规范化；确认 `package.json` 中 `bin` 路径为 `bin/prd-keeper.js` 后复跑，通过发布模拟，仅剩未登录 npm 的 dry-run 提醒。

### 风险/后续事项
- 当前 shell PATH 中未找到系统 Python；用户本机发布/使用前需要安装 Python >= 3.9，或设置 `PRD_KEEPER_PYTHON` 指向真实 Python 可执行文件。
- npm 包名当前为未加 scope 的 `prd-keeper-bootstrap`；本次查询未找到公开包，但正式发布前仍需要由用户账号确认包名、scope 和发布权限。
- npm 正式发布需要用户手动登录 npm 账号，确认 2FA/token、包名/scope、版本号和发布权限后执行 `npm publish`。

## 2026-07-04 - 清理 GitHub README 的受众边界

### 编写人
- dumpling

### 请求
- GitHub 首页 README 应面向外部 GitHub/npm 使用者，避免展示写给本项目研发者的内部 Roadmap、发布维护事项和治理待确认内容；这些信息应尽量保留在项目自身的 PRD Keeper 治理过程中。

### 影响模块
- Bootstrap 安装器。
- README 首页文档。
- 产品概览、Bootstrap 安装器模块文档与可复用坑点。

### 代码变更
- 未修改运行代码。
- 从 README 移除内部 Roadmap 段落。
- 将 README 中“如果你正在本仓库内开发或还没有发布 npm 包”调整为中性的“也可以从源码使用”。
- 将 README License 段落调整为面向使用者的 GPL-3.0 License 说明，不再写维护者发布一致性提醒。

### PRD 更新
- 更新 `docs/prd/product-overview.md`，明确 GitHub 首页 README 面向外部使用者，内部 Roadmap、发布账号/scope、治理待确认事项和任务过程应沉淀在 `docs/prd/` 与 PRD 日志中。
- 更新 `docs/prd/modules/bootstrap-installer.md`，记录 README 只展示安装、初始化、校验和日常使用的当前边界。
- 更新 `.agent/skills/prd-keeper/references/gotchas.md`，新增可复用坑点：不要把内部 Roadmap、发布事项或一次性任务过程写入 GitHub 首页 README。

### 验证
- 已运行：`python .agent\skills\prd-keeper\scripts\check_prd_keeper.py --log-file docs\prd\prd_log\20260704-dumpling.md`，结果为 `prd-keeper validation passed`。

### 风险/后续事项
- 后续如果需要公开 Roadmap，应另行设计面向用户的 Roadmap 文案，并与 PRD 中的内部待确认事项区分。

## 2026-07-04 - 核验 npm 发布结果

### 编写人
- dumpling

### 请求
- 用户完成 npm 发布后，核验 `prd-keeper-bootstrap` 是否已在 npm registry 可见，并确认包元数据、命令映射和 tarball 内容。

### 影响模块
- Bootstrap 安装器。
- npm 分发状态。
- 产品概览与 Bootstrap 安装器模块文档。

### 代码变更
- 未修改运行代码。

### PRD 更新
- 更新 `docs/prd/README.md`，移除 npm 包名/scope 的发布前待确认项。
- 更新 `docs/prd/product-overview.md` 与 `docs/prd/modules/bootstrap-installer.md`，记录 npm 包已作为公开包 `prd-keeper-bootstrap@0.1.0` 发布，`latest` 指向 `0.1.0`。

### 验证
- 已运行：`npm.cmd view prd-keeper-bootstrap version`，结果为 `0.1.0`。
- 已运行：`npm.cmd view prd-keeper-bootstrap name version bin license dist.tarball dist.integrity --json`，确认包名、版本、GPL-3.0-only 许可证、tarball URL 和 `prd-keeper` / `prd-keeper-bootstrap` bin 映射存在。
- 已运行：`npm.cmd view prd-keeper-bootstrap dist-tags time --json`，确认 `latest` 指向 `0.1.0`，发布时间为 `2026-07-04T14:10:13.624Z`。
- 已运行：`npm.cmd pack prd-keeper-bootstrap@latest --dry-run`，确认远端 tarball 包含 30 个文件，包括 `bin/`、`scripts/`、`templates/`、README、LICENSE、根 `SKILL.md` 和根 `routing.yaml`。

### 风险/后续事项
- 本次未执行全局安装验证；如需端到端验证，可运行 `npm install -g prd-keeper-bootstrap` 后执行 `prd-keeper --help` 和一次临时目录初始化。
- 后续发布新版本时必须递增 `package.json` 的 `version`，例如 `0.1.1` 或 `0.2.0`。

## 2026-07-04 - 品牌化 GitHub README 顶部展示

### 编写人
- dumpling

### 请求
- 先生成一个类似示例项目风格的 logo；演示图暂时忽略，其他 README 展示优化先完成。

### 影响模块
- Bootstrap 安装器。
- README 首页文档。
- npm 发布文件集合。
- 产品概览、架构说明、模块索引、Bootstrap 安装器模块文档和项目地图。

### 代码变更
- 新增 `assets/prd-keeper-logo.svg`，作为 GitHub/npm README 顶部品牌展示 logo。
- 更新 README 顶部为居中 logo、npm/version/download/license/Node/Python badges、快捷导航和一句话定位。
- 更新 `package.json` 的 `files`，将 `assets/` 纳入后续 npm 包发布文件集合。

### PRD 更新
- 更新 `docs/prd/README.md`，补充 GitHub/npm 展示入口。
- 更新 `docs/prd/product-overview.md`，记录 README 当前展示结构和外部使用者边界。
- 更新 `docs/prd/architecture.md`，记录 `assets/prd-keeper-logo.svg` 和包含 `assets/` 的发布文件集合。
- 更新 `docs/prd/modules/README.md` 与 `docs/prd/modules/bootstrap-installer.md`，将 `assets/` 和 README 品牌化展示纳入 Bootstrap 安装器模块当前行为。
- 更新 `.agent/skills/prd-keeper/references/project-map.md`，记录新增 logo 资产。

### 验证
- 已运行：`python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260704-dumpling.md`，结果为 `prd-keeper validation passed`。
- 已运行：`npm.cmd pack --dry-run`，确认后续 npm 包会包含 `assets/prd-keeper-logo.svg`，总文件数为 31 个。

### 风险/后续事项
- 本次暂未制作演示图。
- 当前 npm registry 上已发布的 `0.1.0` 包不会自动包含这次 README/logo 变化；如果希望 npm 包页面同步更新，需要后续递增版本后重新发布。

## 2026-07-04 - 准备 0.1.1 npm 补丁发布与 GitHub 元数据

### 编写人
- dumpling

### 请求
- 说明如何让 npm 页面显示新版 README/logo，并替换 README 中的 GitHub 仓库占位地址；先完成本地能完成的部分，发布侧需要用户处理的事项单独说明。

### 影响模块
- Bootstrap 安装器。
- README 首页文档。
- npm 包元数据。
- 产品概览、架构说明、Bootstrap 安装器模块文档和项目地图。

### 代码变更
- 将 `package.json` 本地版本从 `0.1.0` 升级为 `0.1.1`，用于下一次 npm 补丁发布。
- 在 `package.json` 中新增 `repository`、`homepage` 和 `bugs` 元数据，指向 `https://github.com/dumpling233/prd-keeper-bootstrap`。
- 将 README 源码安装示例中的 `https://github.com/your-name/prd-keeper-bootstrap.git` 替换为真实仓库地址 `https://github.com/dumpling233/prd-keeper-bootstrap.git`。

### PRD 更新
- 更新 `docs/prd/README.md`，记录 GitHub 源码入口。
- 更新 `docs/prd/product-overview.md`，记录本地 `0.1.1` 发布准备状态、GitHub 元数据和 npm 页面同步边界。
- 更新 `docs/prd/architecture.md` 与 `docs/prd/modules/bootstrap-installer.md`，记录 `package.json` 当前元数据和发布文件集合。
- 更新 `.agent/skills/prd-keeper/references/project-map.md`，记录 `package.json` 当前本地版本与 GitHub 元数据。

### 验证
- 已运行：`node .\\bin\\prd-keeper.js --version`，结果为 `0.1.1`。
- 已运行：`npm.cmd pack --dry-run`，结果显示待发布包为 `prd-keeper-bootstrap@0.1.1`，包含 `assets/prd-keeper-logo.svg`，总文件数为 31 个。
- 已运行：`npm.cmd view prd-keeper-bootstrap version`，registry 当前返回 `0.1.0`，说明新版本尚未发布。
- 已运行：`python .agent\\skills\\prd-keeper\\scripts\\check_prd_keeper.py --log-file docs\\prd\\prd_log\\20260704-dumpling.md`，结果为 `prd-keeper validation passed`。

### 风险/后续事项
- npm registry 当前仍是已发布版本；只有执行 `npm publish` 成功后，npm 页面才会显示新版 README/logo 和 `0.1.1` 元数据。
- 正式发布需要用户本地 npm 登录状态和可能的 2FA 一次性验证码。
